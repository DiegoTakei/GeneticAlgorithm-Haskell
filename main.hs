import Genetic

startProblem :: IO ()
startProblem = do
    -- get capacidadeMochila, fazer interação com user
    sizeBag <- getLine
    -- getitens
    startAlgorithm
 

startAlgorithm :: IO ()  
    
    --get Tamanho da população
    --get Taxa de crossover
    --get Taxa de mutacao
    --go to Fluxo principal, começando pelo startPopulation
    --startPopulation

    
main = do
   putStrLn("Oi")
   startProblem 





